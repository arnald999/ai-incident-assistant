services:
  ai-incident-assistant-api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ai-incident-assistant-api
    ports:
      - "8000:8000"
    env_file:
      - .env
    restart: unless-stopped